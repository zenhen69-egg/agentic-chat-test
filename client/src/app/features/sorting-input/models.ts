export interface SortingInput {
  sorterId: string;
  tagSerialNo: string;
  isComplete: boolean;
}

export interface SortingInputPayload {
  sorter_id: string;
  tag_serial_no: string;
  is_complete: boolean;
}
