export interface UserProfile {
  fullName: string;
  email: string;
  bio: string;
  isComplete: boolean;
}

export interface UserProfilePayload {
  full_name: string;
  email: string;
  bio: string;
  is_complete: boolean;
}
